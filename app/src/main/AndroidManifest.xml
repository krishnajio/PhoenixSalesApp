<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.phoenix.phoenixsalesapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/img_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Show_hatcherydataActivity"></activity>
        <activity android:name=".HatchdataActivity" />
        <activity android:name=".Hatchery_MenuActivity" />
        <activity android:name=".list_depositActivity" />
        <activity android:name=".BankDepositeActivity" />
        <activity android:name=".Image_uploadnewActivity" />
        <activity android:name=".ChickrateState" />
        <activity android:name=".ChicksrateArea" />
        <activity android:name=".SalesAdminMenuctivity" />
        <activity android:name=".DailyExpensesActivity" />
        <activity android:name=".ImageUploadActivity" />
        <activity android:name=".ListDemandActivity" />
        <activity android:name=".ListDMActivity" />
        <activity android:name=".ListTrActivity" />
        <activity android:name=".ChicksDemandActivity" />
        <activity android:name=".SupplyActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".SalesTrackingActivity"
            android:label="@string/title_activity_sales_tracking" />
        <activity android:name=".DisplayTrActivity" />
        <activity
            android:name=".TrActivity"
            android:label="@string/title_activity_tr"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SalesMenuActivity" />
        <activity android:name=".MainActivity1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>